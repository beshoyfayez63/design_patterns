import { Display } from "../interfaces/display";
import { Observer } from "../interfaces/observer";

export class StaticsDisplay implements Observer, Display {
  display(): void {
      
  }
  update(): void {
      
  }
}